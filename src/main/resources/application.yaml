spring:
    application:
        name: yappu-world
    profiles:
        active: local
        group:
            local: console-logging, secret-local
            test: console-logging
            dev: console-logging
            dev-oci: dev-loki
            prod: console-logging
            prod-oci: prod-loki
