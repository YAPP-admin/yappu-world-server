spring:
    application:
        name: yappu-world
    profiles:
        active: local
        group:
            local: console-logging, fcm-local
            dev: console-logging, file-logging, fcm
            prod: file-logging, fcm
