spring:
    application:
        name: yappu-world
    profiles:
        active: local
        group:
            local: console-logging, secret-local
            test: console-logging
            dev: console-logging, file-logging
            prod: file-logging
