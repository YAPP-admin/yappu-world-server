services:
    app:
        image: yappu-world-prod
        container_name: yappu-world-prod
        build:
            context: .
            dockerfile: dockerfile-prod
        ports:
            - '8080:8080'
        environment:
            - JAVA_OPTS=-Xms256m -Xmx256m
        deploy:
            resources:
                limits:
                    cpus: '0.3'
                    memory: 512M
