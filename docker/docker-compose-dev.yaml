services:
    app:
        image: yappu-world-dev
        container_name: yappu-world-dev
        build:
            context: .
            dockerfile: dockerfile-dev
        ports:
            - '8090:8080'
