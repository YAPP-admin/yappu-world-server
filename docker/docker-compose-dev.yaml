services:
    app:
        image: yappu-world-dev
        container_name: yappu-world-dev
        build:
            context: .
            dockerfile: dockerfile-dev
        ports:
            - '8090:8080'
        deploy:
            resources:
                limits:
                    cpus: '0.3'
                    memory: 512M
